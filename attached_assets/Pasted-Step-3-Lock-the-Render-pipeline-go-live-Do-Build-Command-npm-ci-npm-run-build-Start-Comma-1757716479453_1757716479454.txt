Step 3 — Lock the Render pipeline (go live)

Do

Build Command: npm ci && npm run build

Start Command: node server.mjs

Click Clear build cache → Deploy latest.

Verify

Build passes; Vite emits dist; service starts.

Public URL: /healthz and /readyz return 200.

Step 4 — Wire the hotline (answer fast, record reliably)

Do

Twilio Console → your number → A CALL COMES IN: POST to
https://<your-domain>/voice/answer

Fallback URL: a TwiML Bin that <Record>s (safety net).

Verify

Place a live call: hear consent + greeting → beep.

Twilio → Monitor → the call shows Completed, not Failed/No Answer.

Step 5 — Transcribe + email (multilingual-aware)

Do

Ensure envs:
OPENAI_API_KEY, RESEND_API_KEY, EMAIL_FROM, NOTIFY_EMAIL, PUBLIC_BASE_URL

Confirm /voice/recording-status responds 200 immediately and runs the background job (Whisper + summary JSON + Resend).

Verify

Leave a voicemail; receive an email with transcript + summary; server logs show ✓ emailed transcript (…locale…).

Step 6 — EU locale pack (cheap, reliable)

Do

Env:
SUPPORTED_LOCALES=en-GB,fr-FR,de-DE,es-ES,it-IT,nl-NL
VOICE_en_GB=Polly.Brian, VOICE_fr_FR=Polly.Celine, VOICE_de_DE=Polly.Marlene, VOICE_es_ES=Polly.Lucia, VOICE_it_IT=Polly.Carla, VOICE_nl_NL=Polly.Lotte

Verify

Test calls from EU numbers (or simulate FromCountry): hear the matching local voice; emails use localized subject/body.

Step 7 — Turn on autoscale (after green)

Do (Render)

Min=1, Max=5; scale up on CPU>70%/2m or concurrency>25.

Health check path: /readyz.

Enable auto-rollback if 5xx > 2% over 5m.

Verify

Light load test (k6 or internal): instance count rises, then falls back.

(Optional) Agent prompts to automate 1–3 (idempotent)

PATCH-Node

If .nvmrc ≠ v20.19.5, write it.

If Render env NODE_VERSION ≠ 20.19.5, set it.

Confirm node -v = v20.19.5. No other edits.

PATCH-Port

If server.mjs doesn’t call app.listen(process.env.PORT || 5000, '0.0.0.0'), append minimal fix (do not rewrite file).

Replit Secret PORT=5000 (skip if present).

Probe /healthz, /readyz on the single port.

PATCH-Render

Ensure Build: npm ci && npm run build, Start: node server.mjs.

Output: a one-line note with the final commands; no code changes.